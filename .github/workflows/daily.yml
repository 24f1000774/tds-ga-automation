name: Daily Commit - 24f1000774@ds.study.iitm.ac.in

on:
  schedule:
    - cron: '15 10 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  daily-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create daily commit 24f1000774@ds.study.iitm.ac.in
        run: |
          echo "Daily update at $(date)" >> activity_log.txt
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Automated daily commit" || echo "No changes"
          git push
